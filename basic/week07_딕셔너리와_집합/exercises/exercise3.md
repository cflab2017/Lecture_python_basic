# 과제 3. 중복 제거 (순서 유지)

## 목표
리스트에서 중복을 제거하되 **원래 입력 순서**를 유지한다.

## 요구사항
- 입력: `["a", "b", "a", "c", "b", "d"]`
- 출력: `["a", "b", "c", "d"]`
- `set()` 으로만 처리하면 순서가 깨질 수 있음 → 딕셔너리 활용

## 힌트
파이썬 3.7+ 에서 딕셔너리는 입력 순서를 유지합니다.
`dict.fromkeys(리스트)` 활용 가능.
