# 파이썬 강의 - 기초 과정

> 본 과정은 **기초 / 중급 / 고급** 3단계 커리큘럼 중 **기초** 단계입니다.

## 강의 개요

- **대상**: 프로그래밍을 처음 시작하는 입문자
- **기간**: 총 10주 (주 1회, 회당 2시간 / 총 20시간)
- **선수 지식**: 없음 (컴퓨터 기본 사용 능력만 필요)
- **학습 목표**:
  - 파이썬 개발 환경을 직접 구성할 수 있다
  - 변수, 자료형, 연산자를 자유롭게 다룰 수 있다
  - 조건문과 반복문으로 흐름을 제어할 수 있다
  - 리스트·튜플·딕셔너리·집합 등 기본 자료구조를 활용할 수 있다
  - 함수를 정의하고 코드를 재사용할 수 있다
  - 작은 콘솔 프로그램을 스스로 설계하고 구현할 수 있다

> 객체지향, 모듈/패키지, 예외 처리, 파일 입출력은 **중급 과정**에서 다룹니다.
> 데코레이터, 비동기, 외부 라이브러리, 패키징/배포는 **고급 과정**에서 다룹니다.

---

## 주차별 커리큘럼

### 1주차. 파이썬 시작하기

- 프로그래밍 언어와 인터프리터 개념
- 파이썬 소개와 활용 분야
- 개발 환경 설치 (Python, VS Code)
- REPL과 스크립트 실행 차이
- 첫 프로그램: `Hello, World!`
- 주석 작성법
- **실습**: 환경 설정, 자기소개 출력 프로그램

### 2주차. 변수와 자료형

- 변수 개념과 할당
- 기본 자료형: `int`, `float`, `str`, `bool`, `None`
- 형 변환(type casting)
- `type()`으로 타입 확인하기
- 변수 명명 규칙(snake_case)과 예약어
- **실습**: 자료형 확인 퀴즈, 단위 변환기

### 3주차. 연산자와 입출력

- 산술 연산자(`+ - * / // % **`)
- 비교 연산자, 논리 연산자(`and`, `or`, `not`)
- 대입 연산자와 복합 대입(`+=`, `-=` 등)
- `input()`으로 입력 받기
- `print()`와 f-string 포매팅
- **실습**: BMI 계산기, 거스름돈 계산기

### 4주차. 조건문

- `if`, `elif`, `else` 구조
- 들여쓰기 규칙(4 spaces)
- 중첩 조건문
- 조건 표현식(삼항 연산자)
- 진리값(Truthy / Falsy)
- **실습**: 학점 계산기, 윤년 판별기, 가위바위보

### 5주차. 반복문

- `for` 반복문과 `range()`
- `while` 반복문
- `break`, `continue`
- 중첩 반복문
- 반복문 + 조건문 조합 패턴
- **실습**: 구구단 출력, 별 찍기, 숫자 맞추기 게임

### 6주차. 리스트와 튜플

- 리스트 생성, 인덱싱, 슬라이싱
- 자주 쓰는 리스트 메서드: `append`, `insert`, `pop`, `remove`, `sort`, `reverse`
- 리스트 순회와 `enumerate`
- 튜플의 특징과 언패킹
- 리스트 vs 튜플
- **실습**: 학생 점수 관리, 로또 번호 생성기

### 7주차. 딕셔너리와 집합

- 딕셔너리 생성과 키-값 접근
- 자주 쓰는 메서드: `get`, `keys`, `values`, `items`
- 딕셔너리 순회
- 집합(set) 생성과 연산: 합집합, 교집합, 차집합
- 자료구조 선택 기준
- **실습**: 단어 빈도 카운터, 중복 제거기, 간단한 연락처

### 8주차. 문자열 다루기

- 문자열 인덱싱, 슬라이싱
- 자주 쓰는 메서드: `split`, `join`, `strip`, `replace`, `find`, `lower/upper`
- f-string 심화 (정렬, 자릿수, 소수점 형식)
- 이스케이프 문자(`\n`, `\t` 등)
- **실습**: 회문(palindrome) 판별, 텍스트 통계, 간단 암호화

### 9주차. 함수 기초

- 함수 정의(`def`)와 호출
- 매개변수와 인수
- 기본값 매개변수, 키워드 인수
- 반환값과 `return`
- 변수의 스코프(지역 vs 전역)
- 함수로 코드 재사용하기
- **실습**: 계산기 함수 모음, 입력 검증 함수

### 10주차. 기초 종합 실습

- 지금까지 배운 내용 통합
- 작은 콘솔 프로그램 설계 절차
- 디버깅 기초(`print` 디버깅, 에러 메시지 읽기)
- **프로젝트 예시 (택 1)**:
  - 콘솔 기반 To-Do List
  - 단어장 퀴즈 프로그램
  - 간단한 가계부
  - 숫자 야구 게임

---

## 평가 방식

| 항목 | 비중 |
|------|------|
| 출석 | 10% |
| 주차별 실습 과제 | 40% |
| 중간 퀴즈 (5주차) | 20% |
| 최종 실습 (10주차) | 30% |

---

## 다음 단계

기초 과정을 마치면 다음 단계로 넘어갑니다.

- **중급 과정**: 함수 심화, 모듈/패키지, 객체지향, 예외 처리, 파일 입출력, 컴프리헨션·제너레이터
- **고급 과정**: 데코레이터, 타입 힌트, 동시성(asyncio), 테스트, 외부 라이브러리(NumPy/Pandas/Requests), 패키징·배포

---

## 추천 학습 자료

- [Python 공식 튜토리얼 (한국어)](https://docs.python.org/ko/3/tutorial/)
- [점프 투 파이썬](https://wikidocs.net/book/1)
- 『혼자 공부하는 파이썬』, 『Do it! 점프 투 파이썬』
- 연습: [백준 단계별 풀이](https://www.acmicpc.net/step), [프로그래머스 입문](https://school.programmers.co.kr)

---

## 학습 팁

1. **매일 30분 이상 코드 작성** — 양보다 꾸준함
2. **에러 메시지를 끝까지 읽기** — 에러는 학습의 친구
3. **공식 문서와 친해지기** — 검색은 개발자의 핵심 역량
4. **작은 프로그램을 많이 만들기** — 이론보다 실습
5. **손으로 따라 치기** — 복붙은 실력이 되지 않는다
